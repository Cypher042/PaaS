# Controllers Context

## Purpose
Wiring React/Hooks together with the Application Layer Use Cases. Provides validation inputs using Zod.

## Flow
Receives input -> Validates -> Executes 1 or more Use Cases -> Converts using Presenter -> Gives UI-ready data back to React.
